boolean result1 = (a == 10) && (b == 10);
- 두개 이상의 조건식의 경우 괄호 사용을 권장

블락 + 탭, 블락 + 쉬프트 + 탭

계절 구하기, 학점 구하기(if)
- 무한대의 번호를 윗 코드에서부터 제한하는 방식으로
- 보기에 없는 문항까지 고려해서 코딩하기(ex 잘못된 계절, 월)

modular 연산 

int level = 3;
		String right = ""; // 권한
		switch(level) {
		case 1: right += "쓰기";
		case 2: right += "실행";
		case 3: right += "읽기";
		
		}
		
		System.out.println(level);
- 문자열 누적시키는 스킬?

case문
- case문은 case를 최대한 적게 이용하는 것이 핵심.
- break를 입력하고 안하고에 따른 차이가 있어 이를 활용할 수 있음.

나눗셈 (몫, 나머지)
- 몫을 이용한 코드는 데이터를 그룹화하여 처리할 떄 활용 (ex 분기, 점수대)
 -> 나눗셈을 통해 구한 몫은 그룹의 수를 지정한다?
  -> 12 / 4 는 