- CODE는 PK이고, VARCHAR2타입

// WHERE CODE = '1' (원래 사용법)

WHERE CODE = 1 (동작함)
               ↓
WHERE TO_NUMBER(CODE) = 1 (내부동작)
- TO_NUMBER() 함수로 인해 CODE의 인덱스를 사용할 수 없음

- WHERE절 작성 시 가급적 등호 왼쪽은 가공안하는 것이 좋다